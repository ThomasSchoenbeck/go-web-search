node_modules/
# dist/ is generated by `pnpm build` before `go build` embeds it. Only the
# placeholder is tracked, so a clean checkout still compiles.
dist/*
!dist/.gitkeep
# Playwright run artifacts
test-results/
playwright-report/
